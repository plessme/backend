apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: docs
  name: docs-dpl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: docs
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: docs
    spec:
      containers:
      - image: ingress-nginx-svc.kube-system.svc.bongladesch.com/docker-dev/plessme/backend-docs
        imagePullPolicy: IfNotPresent
        name: nginx
        ports:
          - name: http
            protocol: TCP
            containerPort: 80
        resources:
          limits:
            cpu: "1"
            memory: 512Mi
          requests:
            cpu: "0.1"
            memory: 128Mi
