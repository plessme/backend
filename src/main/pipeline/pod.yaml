apiVersion: v1
kind: Pod
metadata:
  labels:
    repository: plessme/backend
spec:
  serviceAccountName: jenkins-sa
  containers:
  - name: buildpipeline
    image: jcr.bongladesch.com/docker/stack/buildpipeline:0.2.0
    imagePullPolicy: IfNotPresent
    command:
    - cat
    tty: true
